import Button from '../../component_library/Button'

export default function Home() {
  return (
    <main>
      <Button look = 'primary'>Log In</Button>
      <Button>Sign Up</Button>
    </main>
  )
}
